# Python
__pycache__/
*.pyc
*.pyo

# WorkBuddy local workspace data (contains credentials / memory / automation config)
.workbuddy/
# Local config with private webhook (keep out of git; see config.example.json)
config.json
# CodeBuddy/IDE working memory (cross-session notes, not for repo)
.codebuddy/
signin.log
