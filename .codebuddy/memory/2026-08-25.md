# 2026-08-25

## workbuddy-auto-signin：新增 Windows 任务计划调度
- 参考 `D:\workspace\trae-daily-checkin` 的 `schtasks` 方案，为本项目补充纯本地定时能力。
- 新增文件：`install-schedule.bat`（注册 `WorkBuddyAutoSignin` 每日任务，默认 00:05，支持自定义时间）、`scheduled-run.bat`（非交互运行器，解析 Python 绝对路径→运行 `signin.py auto`→落日志）、`uninstall-schedule.bat`、`run-once.bat`、`scripts/notify.ps1`（飞书推送，走 `WORKBUDDY_FEISHU_WEBHOOK` 环境变量）、`CHANGELOG.md`；并更新 `README.md` 调度说明与项目结构。
- 原则：`.bat` 保持 ASCII + CRLF 无 BOM，中文提示走 PowerShell（`notify.ps1`），避免 GBK 误解析；全程非交互（无 `pause`/`set /p`）。
- 已提交并推送到 `schalkiii/workbuddy-auto-signin`（commit 4fb2a60）。

## Git 环境备注
- 本机 Windows git 无全局 user.name/email，提交需用临时身份覆盖：`git -c user.name=... -c user.email=... commit`（不修改任何 config 文件，符合"禁止更新 git config"规则）。
- Shell 为 PowerShell Core，`cd /d` 无效，应使用 `Set-Location` / `cd "绝对路径"`。
- 仓库远端实际为 `schalkiii/workbuddy-auto-signin`（非 README 里的 88lin fork）。

## 隐私处理：scheduled-run.bat 硬编码飞书 webhook
- `scheduled-run.bat` 本地版含一行硬编码的私人飞书机器人 webhook（`https://open.feishu.cn/open-apis/bot/v2/hook/cc37bc21-...`），会随仓库外泄。
- 已按用户要求：移除该 webhook 推送代码 → 提交推送（commit d54a91f，远端干净）→ 再在**本地**恢复（仅本地、不提交，工作区为脏状态）。
- 后续切勿把含真实 webhook 的 `scheduled-run.bat` 提交/推送；仓库版本应保持无硬编码 webhook（走环境变量 `WORKBUDDY_FEISHU_WEBHOOK`）。
- 另：`git add -A` 会把 `.codebuddy/` 也提交（本次已误提交 memory 文件）。建议加 `.gitignore` 忽略 `.codebuddy/`，避免后续误推内部文件。

## 合并上游 88lin/workbuddy-auto-signin 到 fork
- 添加 upstream 远程并 fetch：`git remote add upstream https://github.com/88lin/workbuddy-auto-signin.git`（首次 fetch 因 schannel 断流失败，重试成功）。
- 上游领先 9 个提交（相对 origin/main）：README 重构、新增 **silent 模式**（`pythonw`+`signin.log`，零 token 系统级静默）、清理误提交临时文件（`run-silent.bat`/`setup-*.ps1`/`_task_result.txt` 已删）、代码质量修复（`build_headers` 改抛 `ValueError`、加 `URLError` 捕获）、Star History GitHub Action。
- 我们 fork 领先 3 个提交：调度 `.bat` 脚本 + 飞书推送、移除硬编码 webhook、config.json 外部化。
- 合并冲突 3 处，已手动解决并推送（merge commit 41fb242）：
  - `signin.py` ×2：保留上游 try/except `ValueError` + silent 写日志，**合并进** 我们的 endpoint 多级优先级(env>config>session>默认) 与飞书推送（auto 打印+推送、silent 写日志+推送）。
  - `.gitignore`：保留两边规则（`config.json` + `signin.log`）。
  - `README.md`：以**上游版本为基准**，文末追加「本 Fork 额外工具（.bat 任务计划 + config.json）」一节，未丢弃我们保留的调度脚本文档。
- 合并前先把脏改动（memory + config.example.json 的 CRLF 差异）`git stash`，合并提交后 `git stash pop` 恢复。
